version: '3.8'

services:
  flask-app:
    image: alexshevdev/wog_alexshev:v1.3
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8777:8777"
    volumes:
      - "./Scores.txt:/Scores.txt"
